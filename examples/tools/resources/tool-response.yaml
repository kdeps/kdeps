actionId: toolResponse
name: Tool Response
requires:
    - llmWithTools

validations:
  methods: [POST]
  routes: [/api/v1/tools]
apiResponse:
  success: true
  response:
    query: "{{ get('q') }}"
    llm_response: "{{ get('llmWithTools').message.content }}"
    tools_available: true
