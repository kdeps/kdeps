actionId: response
name: Final Response
requires:
    - translate
    - summarize

apiResponse:
  success: true
  response:
    translation: "{{ output('translate') }}"
    summary:     "{{ output('summarize') }}"
