actionId: finalResponse
name: Final Response
requires:
    - logResult
    - addTimestamp
apiResponse:
  success: true
  response:
    upper: "{{ get('upper') }}"
    timestamp: "{{ get('timestamp') }}"
    message: "Hello, {{ get('formattedName') }}! Welcome!"
