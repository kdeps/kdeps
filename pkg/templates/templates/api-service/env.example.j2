# Copy this file to .env and fill in any values you need.
# kdeps reads .env at runtime; variables are available via env('VAR') in resource YAML.

# API server port (must match portNum in workflow.yaml)
API_PORT={{ port }}

# Ollama host - override if Ollama is not running on localhost
# OLLAMA_HOST=http://localhost:11434
