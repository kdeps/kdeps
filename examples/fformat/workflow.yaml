apiVersion: kdeps.io/v1
kind: Workflow

metadata:
  name: fformat
  description: "Format conversion and validation (JSON, YAML, CSV, XML, TOML)"
  version: "1.0.0"
  targetActionId: fformat

settings:
  apiServer:
    hostIp: "0.0.0.0"
    portNum: 16396
    routes:
      - path: /format
        methods: [POST]

  agentSettings:
    timezone: Etc/UTC
    pythonVersion: "3.12"
