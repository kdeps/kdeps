actionId: systemInfo
name: System Information

validations:
  methods: [GET]
  routes: [/api/v1/exec]

exec:
  command: "echo 'System Info:' && uname -a && echo 'Current Directory:' && pwd && echo 'Date:' && date"
  timeout: 10s
