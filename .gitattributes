# Normalize all text files to LF line endings regardless of the checkout
# platform's core.autocrlf setting. Without this, a Windows clone (Git for
# Windows defaults to core.autocrlf=true) silently rewrites every checked-out
# .go/.yaml/.tmpl/.j2/.sh file to CRLF, which breaks go:embed'd templates,
# string-equality assertions, and POSIX shebang scripts.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary

# Specify language for Python files
*.py linguist-language=Python

# Specify language for Go files
*.go linguist-language=Go

# Exclude test files from language statistics
tests/** linguist-vendored
*.test.py linguist-vendored

# Exclude documentation files from language statistics
docs/** linguist-vendored
*.md linguist-vendored

# Exclude internal folder from language statistics
internal/** linguist-vendored

# Exclude pythonsrc folder from language statistics
pythonsrc/** linguist-vendored
