apiVersion: kdeps.io/v1
kind: Resource

metadata:
  actionId: response
  name: response
  description: Return a summary of applied, skipped, and failed jobs.
  requires: [apply-job]

run:
  apiResponse:
    success: true
    response: "{{ get('apply-job') }}"
