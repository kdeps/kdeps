actionId: results
name: Combined Results
requires: [analytics, batchUpdate]

apiResponse:
  success: true
  response:
    analytics: "{{ get('analytics') }}"
    batch_results: "{{ get('batchUpdate') }}"
    timestamp: "{{ info('current_time') }}"
