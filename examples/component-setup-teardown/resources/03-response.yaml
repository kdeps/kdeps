actionId: response
name: Final Response
requires:
    - countIntro
    - countPoem

apiResponse:
  success: true
  response:
    intro_stats: "{{ output('countIntro') }}"
    poem_stats:  "{{ output('countPoem') }}"
