apiVersion: kdeps.io/v1
kind: Resource

metadata:
  actionId: llm
  name: LLM Response

run:

  resources:
    - chat:
        backend: ollama
        model: llama3.2:3b
        messages:
          - role: user
            content: "{{ input('message') }}"
