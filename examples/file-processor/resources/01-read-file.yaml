actionId: readFile
name: Read File Content

exec:
  command: echo "file content loaded"
apiResponse:
  success: true
  response:
    path: "{{ input('filePath') }}"
    preview: "{{ input('fileContent') | truncate(200) }}"
