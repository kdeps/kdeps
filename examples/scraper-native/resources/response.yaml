apiVersion: kdeps.io/v1
kind: Resource
metadata:
  actionId: response
  requires: [summarize]
apiResponse:
  success: true
  response:
    url: "{{ get('url') }}"
    summary: "{{ output('summarize').summary }}"
